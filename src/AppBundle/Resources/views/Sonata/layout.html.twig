{% extends 'SonataAdminBundle::standard_layout.html.twig' %}

{% block javascripts %}
    {{ parent() }}

    <script>
        $(document).ready(function () {
            var options = $.extend($.datepicker.regional[""]);
            options.dateFormat = "dd.mm.yy";
            $(".timestamp-date").datepicker(options);
        });
    </script>
{% endblock %}
